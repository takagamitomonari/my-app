services:
  - type: web
    name: my-app
    env: node
    plan: free
    buildCommand: npm install && npx prisma generate
    startCommand: npx prisma migrate deploy && npm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          databaseName: my-app-db
          databaseProvider: postgresql
          databaseRegion: oregon
